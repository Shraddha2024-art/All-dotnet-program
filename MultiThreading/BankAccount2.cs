namespace MultiThreading
{
    class BankAccount2
    {
        private static decimal_balance;
        private static readonly object _lokObject = new object();
        public BankAccount2(decimal initialBalance)
        {
            balance = Intialize
        }
         Thread 
    }
}