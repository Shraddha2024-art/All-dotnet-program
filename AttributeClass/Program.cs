namespace AttributeClass
{
    internal class Program
    {
        static void Main(string[]args)
        {
            Student s = new Student();
            Type t = s.GetType();
            System.Console.WriteLine("Name of the class from obj s" + s.GetType());
            System.Console.WriteLine("Name of the class from obj s" + t);
            var classAttribute ! = null 
            {
                
            }

        }
    }
}